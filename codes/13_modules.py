# Modules
# Modules are snippets of codes imported from other files in the project

import math
import sys
import random

# we can say
from math import pi
from enum import Enum

# WE can also define an alias
import random as rdm

print(math.pi)
print(random.choice("1234567899487346"))

print(__name__)
